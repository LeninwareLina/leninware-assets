# --------------------------------------------------
#  GENERAL SETTINGS
# --------------------------------------------------

# Enable mock mode (no external API calls).
# true  = use mock data for ingest, transcript, OpenAI, Shotstack, and YouTube upload
# false = use real APIs
USE_MOCK_AI=false

# Require explicit approval to upload videos to YouTube.
# true  = upload videos
# false = skip uploads (recommended for testing)
ENABLE_YOUTUBE_UPLOAD=false


# --------------------------------------------------
#  OpenAI
# --------------------------------------------------
# Only required when USE_MOCK_AI=false
OPENAI_API_KEY=


# --------------------------------------------------
#  TranscriptAPI
# --------------------------------------------------
# Only required when USE_MOCK_AI=false
TRANSCRIPT_API_KEY=


# --------------------------------------------------
#  YouTube Data API (for ingest)
# --------------------------------------------------
# Only required when USE_MOCK_AI=false
YOUTUBE_API_KEY=


# --------------------------------------------------
#  Shotstack (video rendering)
# --------------------------------------------------
# Only required when USE_MOCK_AI=false
SHOTSTACK_API_KEY=


# --------------------------------------------------
#  YouTube Upload OAuth (for auto-upload)
#  These MUST match the config.py variable names:
#     GOOGLE_CLIENT_ID
#     GOOGLE_CLIENT_SECRET
#     GOOGLE_REFRESH_TOKEN
# --------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=